package com.example.bootapp

class NotificationDismissReceiver {
    // todo create a receiver to to capture the notification dismiss action and safe count to db
    // and start worker to reschedule notofication
    // calculate interval and start OneTimeWorkRequestBuilder
}